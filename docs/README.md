# Docs Index

本文档目录用于承载架构、协议、路线图、ADR 以及 AI 协作文档。使用原则如下：

1. `docs/architecture/` 放稳定设计，不放临时讨论。
2. `docs/api/` 放跨模块契约与消息语义。
3. `docs/roadmap/` 放阶段计划和里程碑。
4. `docs/adr/` 放关键架构决策。
5. `docs/ai/` 放 AI Agent 需要的任务边界、交付规则和上下文索引。

建议每次涉及协议、模块边界或关键流程的改动，都同步更新对应文档。
